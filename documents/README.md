# Place handbook files here (PDF, DOCX, TXT, Markdown).
# This directory is mounted read-only into the orchestration container at /documents.
# You can also upload files through the Ingestion UI at http://localhost:8501.
